name: 'Terraform Destroy GCP'

on:
  workflow_call:
  workflow_dispatch:
    inputs:
      confirmation:
        description: 'Digite "DESTROY" para confirmar a destruição da infraestrutura'
        required: true
        default: ''

jobs:
  terraform:
    name: 'Terraform Destroy GCP'
    runs-on: ubuntu-latest
    env:
      # Diretório onde os arquivos do Terraform estão
      TF_WORKING_DIR: 'infra'

    # Permissões necessárias para o OIDC se comunicar com o GCP
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Authenticate to Google Cloud
        id: auth
        uses: 'google-github-actions/auth@v2'
        with:
          # O caminho completo para o seu Workload Identity Provider
          workload_identity_provider: 'projects/${{ secrets.GCP_PROJECT_NUMBER }}/locations/global/workloadIdentityPools/github-pool-globo/providers/github-pool-globo'
          # O e-mail da Service Account que o Terraform irá usar
          service_account: 'github-actions-sa@projeto-globo-489614.iam.gserviceaccount.com'

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2

      - name: Terraform Init
        run: terraform -chdir="${{ env.TF_WORKING_DIR }}" init

      - name: Terraform Destroy
<<<<<<< HEAD
        if: (github.event_name != 'workflow_dispatch' && github.ref == 'refs/heads/main') || (github.event_name == 'workflow_dispatch' && inputs.confirmation == 'DESTROY')
=======
        if: github.ref == 'refs/heads/main'
>>>>>>> parent of 5886733 (implementing a k8s service simple with only one node, to deploy the api, using terraform e provisoning a artfactory to it)
        run: terraform -chdir="${{ env.TF_WORKING_DIR }}" destroy -auto-approve